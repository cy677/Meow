# Meow V6：本地积分宠物与完整场景奖励

## Windows 一键启动与 IP 访问

把**整个项目文件夹**复制到要运行服务的 Windows 电脑，安装 Node.js 22.16 或更高版本，然后双击 [一键启动.cmd](一键启动.cmd)。保留启动窗口，按 `Ctrl+C` 停服。

脚本会监听全部 IPv4 网卡，默认网页端口为 `8792`；首次需要配置防火墙时会出现 Windows 管理员提示，只添加 `MeowPet-TCP-8792` 入站规则，服务本身仍以原用户身份运行。浏览器自动打开；首次使用进入家长初始化页，一次性初始化口令显示在启动窗口中。

外部设备打开：

```text
http://服务器电脑IP:8792/
http://服务器电脑IP:8792/parent.html
```

电脑可以被远程桌面连接，并不代表网页的 `8792` 端口也已开放。如果经过云安全组或路由器，还需要允许 TCP `8792`，或将公网 `8792` 映射到这台电脑的 `8792`。这部分设置在对应云平台/路由器上完成，启动脚本只配置 Windows 本机防火墙。

[pet-settings.json](pet-settings.json) 可修改：

- `port`：网页端口，默认 `8792`。
- `publicIp`：通常留空，程序自动允许本机网卡 IP。如果外网使用的公网 IP 不在本机网卡上，在这里填写该 IP，不含 `http://`、端口或路径。
- `dataDir`：数据目录，默认 `pet/data`；相对项目目录解析，也可以填写 `D:/MeowData` 这样的绝对路径。
- `openBrowser`：是否自动打开浏览器。
- `configureFirewall`：是否自动配置 Windows 防火墙；已由管理员统一管理时可设为 `false`。

已有 `MEOW_PORT`、`MEOW_DATA_DIR`、`MEOW_PUBLIC_IP` 环境变量优先于配置文件；实际数据文件和访问地址始终显示在启动窗口。HTTPS 仍可使用 `MEOW_TLS_CERT`/`MEOW_TLS_KEY`，详见 `pet/PAD.md`。公网 HTTP 会明文传输登录信息，公网使用建议配置 HTTPS。

## 数据位置与清除

正式数据保存在**运行服务的电脑**上，默认是项目内的 `pet/data/pet.sqlite`，不是浏览器缓存。账号、密码、积分、历史记录、收藏、装扮和自定义预设都在这里。运行中可能还有 `pet.sqlite-wal`、`pet.sqlite-shm` 辅助文件，不能只删除其中一个。

要全部重新开始：在启动窗口按 `Ctrl+C` 停服，然后在同一台电脑双击 [清除数据.cmd](清除数据.cmd)。脚本会将数据库及辅助文件移入同一数据目录的 `backups/时间戳/`，并保留其他文件；再次启动后重新设置家长密码和孩子进入码，积分和收藏从初始状态开始。服务仍在运行时，脚本会拒绝清除。

备份不是新账户会继续读取的数据。需要恢复时，先停服，将当前数据库另行保留，再把该次备份中的完整数据库文件移回数据目录。备份包含原账户和家庭记录，勿公开分享。

本次检查时，当前这份项目没有 `pet/data/pet.sqlite`，也没有配置其他数据目录，已经是空数据状态。如果进度在另一台电脑上，需要在那台电脑操作。

## 命令行启动
本包包含源码和已编译的 `pet/dist`，没有打包 Node.js。也可以在项目目录打开 PowerShell：

```powershell
npm run pet:lan
```

本包的生产服务不需要先安装 npm 依赖。只有修改前端源码、重新构建时才执行 `npm ci --ignore-scripts`、`npm run pet:build`。

Pad 和电脑连接同一可互通家庭网络，在 Pad 浏览器输入终端显示的实际服务器 IP：

```text
http://服务器IP:8792/
http://服务器IP:8792/parent.html
```

第一次启动，用终端的一次性初始化口令设置家长密码和孩子进入码。不要把家长密码交给孩子。服务主机不能休眠；命令行启动不会自动配置防火墙，需要手动放行网页端口。HTTPS/IP 和方向传感器配置见 `pet/PAD.md`。

## 更新旧版：必须保留数据
先停止旧服务，复制备份整个旧 `pet/data/` 目录，再将该目录复制到新版相同位置，或用原来的 `MEOW_DATA_DIR`。随后启动。本包没有自带数据库、密码、CA 证书或私钥。
不要在运行中直接覆盖数据库，不要运行 `git clean -fdx`。不要把两个服务同时指向同一份数据库。

首次升级会一次性增加48项场景目录（8项默认配置、37项可兑换场景物品、3项套装）；原20项猫奖励、积分、理由、拥有权和原预设不重置。后续启动不会重复覆盖。原版猫动作的11项额外示例仍由家长点击按钮加入，不与场景迁移混淆。

## 使用
家长页面“奖励预设”可以筛选、新增、复制、修改地板、地毯、猫窝、玩具、天气、灯光、镜头、雾效和场景套装，调整参数后实时预览，点击保存才入本地库。
孩子的横屏“奖励小屋/我的收藏”中使用“场景布置…”分类，六项一页。兑换、应用和卸下沿用真实本地账户权限；套装按整套固定价格购买，不按已拥有成员自动折价。零费用奖励在达到累计门槛后赠送，不自动换装。
单指在玩具上拖动抓取；非玩具区域旋转视角，双指缩放。声音需主动开启。拖拽后的瞬时位置不入账，刷新按已保存的种子重新摆放；拥有权、预设、数量、种子及当前装备始终保存在本地。

## 说明与验证
场景清单、可调项、参数接口、套装和入窝边界：`pet/SCENES.md`。
动作脚本：`pet/MOTION.md`。横屏弹窗：`pet/LANDSCAPE.md`。
此包来自代码提交 `749134398ee665de0e3d8e08ad9dba73a1b4fc10`，由 Actions 的同一源码树生成。64项测试与六组浏览器回归通过；交付时复核64项测试、两个构建，以及无node_modules的生产服务启动、磁盘重启和原有理由查询。没有实体iPad Safari性能/传感器硬件验收，也不能将测试通过视为完整安全审计。

请勿分享真实孩子数据或私钥。原项目许可证、非商业约束与署名文件均保留。
